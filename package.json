{
  "name": "rubykadai",
  "private": true,
  "scripts": {
    "all": "run-p dev:*",
    "dev:client": "yarn --cwd client/app dev",
    "dev:server": "yarn --cwd server/app start:dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
